/**
 * Create list with the following items on it.
 * Example:
 * A = [a, b, c, d]
 * Question:
 * 1. Wtite a function that takes wp index position and swaps elements of the list.
 * swap(1,2)
 * Answer: A = [b, a, c, d]
 * Look how position of the elements are swapped between list A
 */